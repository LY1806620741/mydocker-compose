version: '2.0'
services:
    baota:
        build:
            context: baota/
        container_name: baota
        #host模式不需要定义端口了
        #ports:
        #    - "8888:8888"
        restart: always        
        volumes:
            - ./wwwroot/:/www/wwwroot
        privileged: true
        # tty: true
        networks:
            - host
networks:
    host:
